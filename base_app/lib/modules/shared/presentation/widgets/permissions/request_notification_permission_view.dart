import 'package:flutter/material.dart';

class RequestNotificationPermissionView extends StatelessWidget {
  const RequestNotificationPermissionView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
