import 'package:flutter/material.dart';

class RequestLocationPermisisonView extends StatelessWidget {
  const RequestLocationPermisisonView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
