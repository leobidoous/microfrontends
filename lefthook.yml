source_dir: "./.lefthook"

pre-push:
  parallel: true
  scripts: 
    # "tests_runner":
    #   runner: bash
    # "analyze_runner":
    #   runner: bash
pre-commit:
  commands:
    pretty:
      glob: "*.dart"
      run: flutter format {staged_files} && git add {staged_files}
commit-msg:
  scripts:
    "commit-msg/template_checker":
      runner: bash